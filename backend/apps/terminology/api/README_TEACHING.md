# terminology/api 目录教程

- `terminology.py`：术语分页、创建、编辑、删除、批量导入导出和 embedding 更新接口。
- `__init__.py`：包声明。

接口覆盖分页、详情、创建、更新、批量导入、删除、启停和导出，并允许按数据源范围筛选。当前工作空间来自 CurrentUser，不能信任客户端任意传入 oid。

批量导入应区分格式错误、重复术语和 embedding 失败；审计日志记录管理动作，但不得把不必要的大批量原文全部塞入日志。

